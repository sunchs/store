server:
  port: 8003
spring:
  application:
    name: receive-service
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: Lyt@2019
    url: jdbc:mysql://47.107.255.115:3306/hospital??useUnicode=true&characterEncoding=utf8&useSSL=false

eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 10
  client:
    serviceUrl:
      defaultZone: http://47.107.255.115:6006/eureka/

#logging:
#  level:
#    com.sunchs.store.db.business.mapper: DEBUG
#    com.sunchs.store.db.report.mapper: DEBUG